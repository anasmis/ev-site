// Create placeholder pages for all routes
import React from 'react'
import { Container, Row, Col } from 'react-bootstrap'

// Re-export the real ProductDetail component
export { default as ProductDetail } from './ProductDetail'



// Re-export the real Checkout component
export { default as Checkout } from './Checkout'

// Re-export the real Profile component
export { default as Profile } from './Profile'

// Re-export the real Orders component
export { default as Orders } from './Orders'

// Re-export the real OrderDetail component
export { default as OrderDetail } from './OrderDetail'

// Re-export the real Wishlist component
export { default as Wishlist } from './Wishlist'

// Re-export the real About component
export { default as About } from './About'

// Re-export the real FAQ component
export { default as FAQ } from './FAQ'

// Re-export the real StationsMap component
export { default as StationsMap } from './StationsMap'
